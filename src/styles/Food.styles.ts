import styled from "styled-components";

export const FoodPage = styled.main`
  position: relative;
  min-height: 100%;
`;

export const FixedTopArea = styled.div`
  flex-shrink: 0;
`;

export const ListArea = styled.div``;
